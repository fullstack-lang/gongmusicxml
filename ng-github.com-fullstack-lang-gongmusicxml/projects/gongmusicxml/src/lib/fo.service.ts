// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule, HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { FoAPI } from './fo-api'
import { Fo, CopyFoToFoAPI } from './fo'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class FoService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  FoServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private fosUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.fosUrl = origin + '/api/github.com/fullstack-lang/gongmusicxml/go/v1/fos';
  }

  /** GET fos from the server */
  // gets is more robust to refactoring
  gets(GONG__StackPath: string, frontRepo: FrontRepo): Observable<FoAPI[]> {
    return this.getFos(GONG__StackPath, frontRepo)
  }
  getFos(GONG__StackPath: string, frontRepo: FrontRepo): Observable<FoAPI[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<FoAPI[]>(this.fosUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<FoAPI[]>('getFos', []))
      );
  }

  /** GET fo by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<FoAPI> {
    return this.getFo(id, GONG__StackPath, frontRepo)
  }
  getFo(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<FoAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.fosUrl}/${id}`;
    return this.http.get<FoAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched fo id=${id}`)),
      catchError(this.handleError<FoAPI>(`getFo id=${id}`))
    );
  }

  // postFront copy fo to a version with encoded pointers and post to the back
  postFront(fo: Fo, GONG__StackPath: string): Observable<FoAPI> {
    let foAPI = new FoAPI
    CopyFoToFoAPI(fo, foAPI)
    const id = typeof foAPI === 'number' ? foAPI : foAPI.ID
    const url = `${this.fosUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<FoAPI>(url, foAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<FoAPI>('postFo'))
    );
  }
  
  /** POST: add a new fo to the server */
  post(fodb: FoAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<FoAPI> {
    return this.postFo(fodb, GONG__StackPath, frontRepo)
  }
  postFo(fodb: FoAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<FoAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<FoAPI>(this.fosUrl, fodb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted fodb id=${fodb.ID}`)
      }),
      catchError(this.handleError<FoAPI>('postFo'))
    );
  }

  /** DELETE: delete the fodb from the server */
  delete(fodb: FoAPI | number, GONG__StackPath: string): Observable<FoAPI> {
    return this.deleteFo(fodb, GONG__StackPath)
  }
  deleteFo(fodb: FoAPI | number, GONG__StackPath: string): Observable<FoAPI> {
    const id = typeof fodb === 'number' ? fodb : fodb.ID;
    const url = `${this.fosUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<FoAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted fodb id=${id}`)),
      catchError(this.handleError<FoAPI>('deleteFo'))
    );
  }

  // updateFront copy fo to a version with encoded pointers and update to the back
  updateFront(fo: Fo, GONG__StackPath: string): Observable<FoAPI> {
    let foAPI = new FoAPI
    CopyFoToFoAPI(fo, foAPI)
    const id = typeof foAPI === 'number' ? foAPI : foAPI.ID
    const url = `${this.fosUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<FoAPI>(url, foAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<FoAPI>('updateFo'))
    );
  }

  /** PUT: update the fodb on the server */
  update(fodb: FoAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<FoAPI> {
    return this.updateFo(fodb, GONG__StackPath, frontRepo)
  }
  updateFo(fodb: FoAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<FoAPI> {
    const id = typeof fodb === 'number' ? fodb : fodb.ID;
    const url = `${this.fosUrl}/${id}`;


    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<FoAPI>(url, fodb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated fodb id=${fodb.ID}`)
      }),
      catchError(this.handleError<FoAPI>('updateFo'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in FoService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("FoService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
