// generated by stacks/gong/go/models/orm_file_per_struct_back_repo.go
package orm

import (
	"database/sql"
	"encoding/json"
	"errors"
	"fmt"
	"io/ioutil"
	"log"
	"os"
	"path/filepath"
	"sort"
	"time"

	"gorm.io/gorm"

	"github.com/tealeg/xlsx/v3"

	"github.com/fullstack-lang/gongmusicxml/go/models"
)

// dummy variable to have the import declaration wihthout compile failure (even if no code needing this import is generated)
var dummy_Other_appearance_sql sql.NullBool
var dummy_Other_appearance_time time.Duration
var dummy_Other_appearance_sort sort.Float64Slice

// Other_appearanceAPI is the input in POST API
//
// for POST, API, one needs the fields of the model as well as the fields
// from associations ("Has One" and "Has Many") that are generated to
// fullfill the ORM requirements for associations
//
// swagger:model other_appearanceAPI
type Other_appearanceAPI struct {
	gorm.Model

	models.Other_appearance_WOP

	// encoding of pointers
	// for API, it cannot be embedded
	Other_appearancePointersEncoding Other_appearancePointersEncoding
}

// Other_appearancePointersEncoding encodes pointers to Struct and
// reverse pointers of slice of poitners to Struct
type Other_appearancePointersEncoding struct {
	// insertion for pointer fields encoding declaration
}

// Other_appearanceDB describes a other_appearance in the database
//
// It incorporates the GORM ID, basic fields from the model (because they can be serialized),
// the encoded version of pointers
//
// swagger:model other_appearanceDB
type Other_appearanceDB struct {
	gorm.Model

	// insertion for basic fields declaration

	// Declation for basic field other_appearanceDB.Name
	Name_Data sql.NullString

	// Declation for basic field other_appearanceDB.Value
	Value_Data sql.NullString

	// Declation for basic field other_appearanceDB.Type
	Type_Data sql.NullString
	
	// encoding of pointers
	// for GORM serialization, it is necessary to embed to Pointer Encoding declaration
	Other_appearancePointersEncoding
}

// Other_appearanceDBs arrays other_appearanceDBs
// swagger:response other_appearanceDBsResponse
type Other_appearanceDBs []Other_appearanceDB

// Other_appearanceDBResponse provides response
// swagger:response other_appearanceDBResponse
type Other_appearanceDBResponse struct {
	Other_appearanceDB
}

// Other_appearanceWOP is a Other_appearance without pointers (WOP is an acronym for "Without Pointers")
// it holds the same basic fields but pointers are encoded into uint
type Other_appearanceWOP struct {
	ID int `xlsx:"0"`

	// insertion for WOP basic fields

	Name string `xlsx:"1"`

	Value string `xlsx:"2"`

	Type string `xlsx:"3"`
	// insertion for WOP pointer fields
}

var Other_appearance_Fields = []string{
	// insertion for WOP basic fields
	"ID",
	"Name",
	"Value",
	"Type",
}

type BackRepoOther_appearanceStruct struct {
	// stores Other_appearanceDB according to their gorm ID
	Map_Other_appearanceDBID_Other_appearanceDB map[uint]*Other_appearanceDB

	// stores Other_appearanceDB ID according to Other_appearance address
	Map_Other_appearancePtr_Other_appearanceDBID map[*models.Other_appearance]uint

	// stores Other_appearance according to their gorm ID
	Map_Other_appearanceDBID_Other_appearancePtr map[uint]*models.Other_appearance

	db *gorm.DB

	stage *models.StageStruct
}

func (backRepoOther_appearance *BackRepoOther_appearanceStruct) GetStage() (stage *models.StageStruct) {
	stage = backRepoOther_appearance.stage
	return
}

func (backRepoOther_appearance *BackRepoOther_appearanceStruct) GetDB() *gorm.DB {
	return backRepoOther_appearance.db
}

// GetOther_appearanceDBFromOther_appearancePtr is a handy function to access the back repo instance from the stage instance
func (backRepoOther_appearance *BackRepoOther_appearanceStruct) GetOther_appearanceDBFromOther_appearancePtr(other_appearance *models.Other_appearance) (other_appearanceDB *Other_appearanceDB) {
	id := backRepoOther_appearance.Map_Other_appearancePtr_Other_appearanceDBID[other_appearance]
	other_appearanceDB = backRepoOther_appearance.Map_Other_appearanceDBID_Other_appearanceDB[id]
	return
}

// BackRepoOther_appearance.CommitPhaseOne commits all staged instances of Other_appearance to the BackRepo
// Phase One is the creation of instance in the database if it is not yet done to get the unique ID for each staged instance
func (backRepoOther_appearance *BackRepoOther_appearanceStruct) CommitPhaseOne(stage *models.StageStruct) (Error error) {

	for other_appearance := range stage.Other_appearances {
		backRepoOther_appearance.CommitPhaseOneInstance(other_appearance)
	}

	// parse all backRepo instance and checks wether some instance have been unstaged
	// in this case, remove them from the back repo
	for id, other_appearance := range backRepoOther_appearance.Map_Other_appearanceDBID_Other_appearancePtr {
		if _, ok := stage.Other_appearances[other_appearance]; !ok {
			backRepoOther_appearance.CommitDeleteInstance(id)
		}
	}

	return
}

// BackRepoOther_appearance.CommitDeleteInstance commits deletion of Other_appearance to the BackRepo
func (backRepoOther_appearance *BackRepoOther_appearanceStruct) CommitDeleteInstance(id uint) (Error error) {

	other_appearance := backRepoOther_appearance.Map_Other_appearanceDBID_Other_appearancePtr[id]

	// other_appearance is not staged anymore, remove other_appearanceDB
	other_appearanceDB := backRepoOther_appearance.Map_Other_appearanceDBID_Other_appearanceDB[id]
	query := backRepoOther_appearance.db.Unscoped().Delete(&other_appearanceDB)
	if query.Error != nil {
		log.Fatal(query.Error)
	}

	// update stores
	delete(backRepoOther_appearance.Map_Other_appearancePtr_Other_appearanceDBID, other_appearance)
	delete(backRepoOther_appearance.Map_Other_appearanceDBID_Other_appearancePtr, id)
	delete(backRepoOther_appearance.Map_Other_appearanceDBID_Other_appearanceDB, id)

	return
}

// BackRepoOther_appearance.CommitPhaseOneInstance commits other_appearance staged instances of Other_appearance to the BackRepo
// Phase One is the creation of instance in the database if it is not yet done to get the unique ID for each staged instance
func (backRepoOther_appearance *BackRepoOther_appearanceStruct) CommitPhaseOneInstance(other_appearance *models.Other_appearance) (Error error) {

	// check if the other_appearance is not commited yet
	if _, ok := backRepoOther_appearance.Map_Other_appearancePtr_Other_appearanceDBID[other_appearance]; ok {
		return
	}

	// initiate other_appearance
	var other_appearanceDB Other_appearanceDB
	other_appearanceDB.CopyBasicFieldsFromOther_appearance(other_appearance)

	query := backRepoOther_appearance.db.Create(&other_appearanceDB)
	if query.Error != nil {
		log.Fatal(query.Error)
	}

	// update stores
	backRepoOther_appearance.Map_Other_appearancePtr_Other_appearanceDBID[other_appearance] = other_appearanceDB.ID
	backRepoOther_appearance.Map_Other_appearanceDBID_Other_appearancePtr[other_appearanceDB.ID] = other_appearance
	backRepoOther_appearance.Map_Other_appearanceDBID_Other_appearanceDB[other_appearanceDB.ID] = &other_appearanceDB

	return
}

// BackRepoOther_appearance.CommitPhaseTwo commits all staged instances of Other_appearance to the BackRepo
// Phase Two is the update of instance with the field in the database
func (backRepoOther_appearance *BackRepoOther_appearanceStruct) CommitPhaseTwo(backRepo *BackRepoStruct) (Error error) {

	for idx, other_appearance := range backRepoOther_appearance.Map_Other_appearanceDBID_Other_appearancePtr {
		backRepoOther_appearance.CommitPhaseTwoInstance(backRepo, idx, other_appearance)
	}

	return
}

// BackRepoOther_appearance.CommitPhaseTwoInstance commits {{structname }} of models.Other_appearance to the BackRepo
// Phase Two is the update of instance with the field in the database
func (backRepoOther_appearance *BackRepoOther_appearanceStruct) CommitPhaseTwoInstance(backRepo *BackRepoStruct, idx uint, other_appearance *models.Other_appearance) (Error error) {

	// fetch matching other_appearanceDB
	if other_appearanceDB, ok := backRepoOther_appearance.Map_Other_appearanceDBID_Other_appearanceDB[idx]; ok {

		other_appearanceDB.CopyBasicFieldsFromOther_appearance(other_appearance)

		// insertion point for translating pointers encodings into actual pointers
		query := backRepoOther_appearance.db.Save(&other_appearanceDB)
		if query.Error != nil {
			log.Fatalln(query.Error)
		}

	} else {
		err := errors.New(
			fmt.Sprintf("Unkown Other_appearance intance %s", other_appearance.Name))
		return err
	}

	return
}

// BackRepoOther_appearance.CheckoutPhaseOne Checkouts all BackRepo instances to the Stage
//
// Phase One will result in having instances on the stage aligned with the back repo
// pointers are not initialized yet (this is for phase two)
func (backRepoOther_appearance *BackRepoOther_appearanceStruct) CheckoutPhaseOne() (Error error) {

	other_appearanceDBArray := make([]Other_appearanceDB, 0)
	query := backRepoOther_appearance.db.Find(&other_appearanceDBArray)
	if query.Error != nil {
		return query.Error
	}

	// list of instances to be removed
	// start from the initial map on the stage and remove instances that have been checked out
	other_appearanceInstancesToBeRemovedFromTheStage := make(map[*models.Other_appearance]any)
	for key, value := range backRepoOther_appearance.stage.Other_appearances {
		other_appearanceInstancesToBeRemovedFromTheStage[key] = value
	}

	// copy orm objects to the the map
	for _, other_appearanceDB := range other_appearanceDBArray {
		backRepoOther_appearance.CheckoutPhaseOneInstance(&other_appearanceDB)

		// do not remove this instance from the stage, therefore
		// remove instance from the list of instances to be be removed from the stage
		other_appearance, ok := backRepoOther_appearance.Map_Other_appearanceDBID_Other_appearancePtr[other_appearanceDB.ID]
		if ok {
			delete(other_appearanceInstancesToBeRemovedFromTheStage, other_appearance)
		}
	}

	// remove from stage and back repo's 3 maps all other_appearances that are not in the checkout
	for other_appearance := range other_appearanceInstancesToBeRemovedFromTheStage {
		other_appearance.Unstage(backRepoOther_appearance.GetStage())

		// remove instance from the back repo 3 maps
		other_appearanceID := backRepoOther_appearance.Map_Other_appearancePtr_Other_appearanceDBID[other_appearance]
		delete(backRepoOther_appearance.Map_Other_appearancePtr_Other_appearanceDBID, other_appearance)
		delete(backRepoOther_appearance.Map_Other_appearanceDBID_Other_appearanceDB, other_appearanceID)
		delete(backRepoOther_appearance.Map_Other_appearanceDBID_Other_appearancePtr, other_appearanceID)
	}

	return
}

// CheckoutPhaseOneInstance takes a other_appearanceDB that has been found in the DB, updates the backRepo and stages the
// models version of the other_appearanceDB
func (backRepoOther_appearance *BackRepoOther_appearanceStruct) CheckoutPhaseOneInstance(other_appearanceDB *Other_appearanceDB) (Error error) {

	other_appearance, ok := backRepoOther_appearance.Map_Other_appearanceDBID_Other_appearancePtr[other_appearanceDB.ID]
	if !ok {
		other_appearance = new(models.Other_appearance)

		backRepoOther_appearance.Map_Other_appearanceDBID_Other_appearancePtr[other_appearanceDB.ID] = other_appearance
		backRepoOther_appearance.Map_Other_appearancePtr_Other_appearanceDBID[other_appearance] = other_appearanceDB.ID

		// append model store with the new element
		other_appearance.Name = other_appearanceDB.Name_Data.String
		other_appearance.Stage(backRepoOther_appearance.GetStage())
	}
	other_appearanceDB.CopyBasicFieldsToOther_appearance(other_appearance)

	// in some cases, the instance might have been unstaged. It is necessary to stage it again
	other_appearance.Stage(backRepoOther_appearance.GetStage())

	// preserve pointer to other_appearanceDB. Otherwise, pointer will is recycled and the map of pointers
	// Map_Other_appearanceDBID_Other_appearanceDB)[other_appearanceDB hold variable pointers
	other_appearanceDB_Data := *other_appearanceDB
	preservedPtrToOther_appearance := &other_appearanceDB_Data
	backRepoOther_appearance.Map_Other_appearanceDBID_Other_appearanceDB[other_appearanceDB.ID] = preservedPtrToOther_appearance

	return
}

// BackRepoOther_appearance.CheckoutPhaseTwo Checkouts all staged instances of Other_appearance to the BackRepo
// Phase Two is the update of instance with the field in the database
func (backRepoOther_appearance *BackRepoOther_appearanceStruct) CheckoutPhaseTwo(backRepo *BackRepoStruct) (Error error) {

	// parse all DB instance and update all pointer fields of the translated models instance
	for _, other_appearanceDB := range backRepoOther_appearance.Map_Other_appearanceDBID_Other_appearanceDB {
		backRepoOther_appearance.CheckoutPhaseTwoInstance(backRepo, other_appearanceDB)
	}
	return
}

// BackRepoOther_appearance.CheckoutPhaseTwoInstance Checkouts staged instances of Other_appearance to the BackRepo
// Phase Two is the update of instance with the field in the database
func (backRepoOther_appearance *BackRepoOther_appearanceStruct) CheckoutPhaseTwoInstance(backRepo *BackRepoStruct, other_appearanceDB *Other_appearanceDB) (Error error) {

	other_appearance := backRepoOther_appearance.Map_Other_appearanceDBID_Other_appearancePtr[other_appearanceDB.ID]

	other_appearanceDB.DecodePointers(backRepo, other_appearance)

	return
}

func (other_appearanceDB *Other_appearanceDB) DecodePointers(backRepo *BackRepoStruct, other_appearance *models.Other_appearance) {

	// insertion point for checkout of pointer encoding
	return
}

// CommitOther_appearance allows commit of a single other_appearance (if already staged)
func (backRepo *BackRepoStruct) CommitOther_appearance(other_appearance *models.Other_appearance) {
	backRepo.BackRepoOther_appearance.CommitPhaseOneInstance(other_appearance)
	if id, ok := backRepo.BackRepoOther_appearance.Map_Other_appearancePtr_Other_appearanceDBID[other_appearance]; ok {
		backRepo.BackRepoOther_appearance.CommitPhaseTwoInstance(backRepo, id, other_appearance)
	}
	backRepo.CommitFromBackNb = backRepo.CommitFromBackNb + 1
}

// CommitOther_appearance allows checkout of a single other_appearance (if already staged and with a BackRepo id)
func (backRepo *BackRepoStruct) CheckoutOther_appearance(other_appearance *models.Other_appearance) {
	// check if the other_appearance is staged
	if _, ok := backRepo.BackRepoOther_appearance.Map_Other_appearancePtr_Other_appearanceDBID[other_appearance]; ok {

		if id, ok := backRepo.BackRepoOther_appearance.Map_Other_appearancePtr_Other_appearanceDBID[other_appearance]; ok {
			var other_appearanceDB Other_appearanceDB
			other_appearanceDB.ID = id

			if err := backRepo.BackRepoOther_appearance.db.First(&other_appearanceDB, id).Error; err != nil {
				log.Fatalln("CheckoutOther_appearance : Problem with getting object with id:", id)
			}
			backRepo.BackRepoOther_appearance.CheckoutPhaseOneInstance(&other_appearanceDB)
			backRepo.BackRepoOther_appearance.CheckoutPhaseTwoInstance(backRepo, &other_appearanceDB)
		}
	}
}

// CopyBasicFieldsFromOther_appearance
func (other_appearanceDB *Other_appearanceDB) CopyBasicFieldsFromOther_appearance(other_appearance *models.Other_appearance) {
	// insertion point for fields commit

	other_appearanceDB.Name_Data.String = other_appearance.Name
	other_appearanceDB.Name_Data.Valid = true

	other_appearanceDB.Value_Data.String = other_appearance.Value
	other_appearanceDB.Value_Data.Valid = true

	other_appearanceDB.Type_Data.String = other_appearance.Type
	other_appearanceDB.Type_Data.Valid = true
}

// CopyBasicFieldsFromOther_appearance_WOP
func (other_appearanceDB *Other_appearanceDB) CopyBasicFieldsFromOther_appearance_WOP(other_appearance *models.Other_appearance_WOP) {
	// insertion point for fields commit

	other_appearanceDB.Name_Data.String = other_appearance.Name
	other_appearanceDB.Name_Data.Valid = true

	other_appearanceDB.Value_Data.String = other_appearance.Value
	other_appearanceDB.Value_Data.Valid = true

	other_appearanceDB.Type_Data.String = other_appearance.Type
	other_appearanceDB.Type_Data.Valid = true
}

// CopyBasicFieldsFromOther_appearanceWOP
func (other_appearanceDB *Other_appearanceDB) CopyBasicFieldsFromOther_appearanceWOP(other_appearance *Other_appearanceWOP) {
	// insertion point for fields commit

	other_appearanceDB.Name_Data.String = other_appearance.Name
	other_appearanceDB.Name_Data.Valid = true

	other_appearanceDB.Value_Data.String = other_appearance.Value
	other_appearanceDB.Value_Data.Valid = true

	other_appearanceDB.Type_Data.String = other_appearance.Type
	other_appearanceDB.Type_Data.Valid = true
}

// CopyBasicFieldsToOther_appearance
func (other_appearanceDB *Other_appearanceDB) CopyBasicFieldsToOther_appearance(other_appearance *models.Other_appearance) {
	// insertion point for checkout of basic fields (back repo to stage)
	other_appearance.Name = other_appearanceDB.Name_Data.String
	other_appearance.Value = other_appearanceDB.Value_Data.String
	other_appearance.Type = other_appearanceDB.Type_Data.String
}

// CopyBasicFieldsToOther_appearance_WOP
func (other_appearanceDB *Other_appearanceDB) CopyBasicFieldsToOther_appearance_WOP(other_appearance *models.Other_appearance_WOP) {
	// insertion point for checkout of basic fields (back repo to stage)
	other_appearance.Name = other_appearanceDB.Name_Data.String
	other_appearance.Value = other_appearanceDB.Value_Data.String
	other_appearance.Type = other_appearanceDB.Type_Data.String
}

// CopyBasicFieldsToOther_appearanceWOP
func (other_appearanceDB *Other_appearanceDB) CopyBasicFieldsToOther_appearanceWOP(other_appearance *Other_appearanceWOP) {
	other_appearance.ID = int(other_appearanceDB.ID)
	// insertion point for checkout of basic fields (back repo to stage)
	other_appearance.Name = other_appearanceDB.Name_Data.String
	other_appearance.Value = other_appearanceDB.Value_Data.String
	other_appearance.Type = other_appearanceDB.Type_Data.String
}

// Backup generates a json file from a slice of all Other_appearanceDB instances in the backrepo
func (backRepoOther_appearance *BackRepoOther_appearanceStruct) Backup(dirPath string) {

	filename := filepath.Join(dirPath, "Other_appearanceDB.json")

	// organize the map into an array with increasing IDs, in order to have repoductible
	// backup file
	forBackup := make([]*Other_appearanceDB, 0)
	for _, other_appearanceDB := range backRepoOther_appearance.Map_Other_appearanceDBID_Other_appearanceDB {
		forBackup = append(forBackup, other_appearanceDB)
	}

	sort.Slice(forBackup[:], func(i, j int) bool {
		return forBackup[i].ID < forBackup[j].ID
	})

	file, err := json.MarshalIndent(forBackup, "", " ")

	if err != nil {
		log.Fatal("Cannot json Other_appearance ", filename, " ", err.Error())
	}

	err = ioutil.WriteFile(filename, file, 0644)
	if err != nil {
		log.Fatal("Cannot write the json Other_appearance file", err.Error())
	}
}

// Backup generates a json file from a slice of all Other_appearanceDB instances in the backrepo
func (backRepoOther_appearance *BackRepoOther_appearanceStruct) BackupXL(file *xlsx.File) {

	// organize the map into an array with increasing IDs, in order to have repoductible
	// backup file
	forBackup := make([]*Other_appearanceDB, 0)
	for _, other_appearanceDB := range backRepoOther_appearance.Map_Other_appearanceDBID_Other_appearanceDB {
		forBackup = append(forBackup, other_appearanceDB)
	}

	sort.Slice(forBackup[:], func(i, j int) bool {
		return forBackup[i].ID < forBackup[j].ID
	})

	sh, err := file.AddSheet("Other_appearance")
	if err != nil {
		log.Fatal("Cannot add XL file", err.Error())
	}
	_ = sh

	row := sh.AddRow()
	row.WriteSlice(&Other_appearance_Fields, -1)
	for _, other_appearanceDB := range forBackup {

		var other_appearanceWOP Other_appearanceWOP
		other_appearanceDB.CopyBasicFieldsToOther_appearanceWOP(&other_appearanceWOP)

		row := sh.AddRow()
		row.WriteStruct(&other_appearanceWOP, -1)
	}
}

// RestoreXL from the "Other_appearance" sheet all Other_appearanceDB instances
func (backRepoOther_appearance *BackRepoOther_appearanceStruct) RestoreXLPhaseOne(file *xlsx.File) {

	// resets the map
	BackRepoOther_appearanceid_atBckpTime_newID = make(map[uint]uint)

	sh, ok := file.Sheet["Other_appearance"]
	_ = sh
	if !ok {
		log.Fatal(errors.New("sheet not found"))
	}

	// log.Println("Max row is", sh.MaxRow)
	err := sh.ForEachRow(backRepoOther_appearance.rowVisitorOther_appearance)
	if err != nil {
		log.Fatal("Err=", err)
	}
}

func (backRepoOther_appearance *BackRepoOther_appearanceStruct) rowVisitorOther_appearance(row *xlsx.Row) error {

	log.Printf("row line %d\n", row.GetCoordinate())
	log.Println(row)

	// skip first line
	if row.GetCoordinate() > 0 {
		var other_appearanceWOP Other_appearanceWOP
		row.ReadStruct(&other_appearanceWOP)

		// add the unmarshalled struct to the stage
		other_appearanceDB := new(Other_appearanceDB)
		other_appearanceDB.CopyBasicFieldsFromOther_appearanceWOP(&other_appearanceWOP)

		other_appearanceDB_ID_atBackupTime := other_appearanceDB.ID
		other_appearanceDB.ID = 0
		query := backRepoOther_appearance.db.Create(other_appearanceDB)
		if query.Error != nil {
			log.Fatal(query.Error)
		}
		backRepoOther_appearance.Map_Other_appearanceDBID_Other_appearanceDB[other_appearanceDB.ID] = other_appearanceDB
		BackRepoOther_appearanceid_atBckpTime_newID[other_appearanceDB_ID_atBackupTime] = other_appearanceDB.ID
	}
	return nil
}

// RestorePhaseOne read the file "Other_appearanceDB.json" in dirPath that stores an array
// of Other_appearanceDB and stores it in the database
// the map BackRepoOther_appearanceid_atBckpTime_newID is updated accordingly
func (backRepoOther_appearance *BackRepoOther_appearanceStruct) RestorePhaseOne(dirPath string) {

	// resets the map
	BackRepoOther_appearanceid_atBckpTime_newID = make(map[uint]uint)

	filename := filepath.Join(dirPath, "Other_appearanceDB.json")
	jsonFile, err := os.Open(filename)
	// if we os.Open returns an error then handle it
	if err != nil {
		log.Fatal("Cannot restore/open the json Other_appearance file", filename, " ", err.Error())
	}

	// read our opened jsonFile as a byte array.
	byteValue, _ := ioutil.ReadAll(jsonFile)

	var forRestore []*Other_appearanceDB

	err = json.Unmarshal(byteValue, &forRestore)

	// fill up Map_Other_appearanceDBID_Other_appearanceDB
	for _, other_appearanceDB := range forRestore {

		other_appearanceDB_ID_atBackupTime := other_appearanceDB.ID
		other_appearanceDB.ID = 0
		query := backRepoOther_appearance.db.Create(other_appearanceDB)
		if query.Error != nil {
			log.Fatal(query.Error)
		}
		backRepoOther_appearance.Map_Other_appearanceDBID_Other_appearanceDB[other_appearanceDB.ID] = other_appearanceDB
		BackRepoOther_appearanceid_atBckpTime_newID[other_appearanceDB_ID_atBackupTime] = other_appearanceDB.ID
	}

	if err != nil {
		log.Fatal("Cannot restore/unmarshall json Other_appearance file", err.Error())
	}
}

// RestorePhaseTwo uses all map BackRepo<Other_appearance>id_atBckpTime_newID
// to compute new index
func (backRepoOther_appearance *BackRepoOther_appearanceStruct) RestorePhaseTwo() {

	for _, other_appearanceDB := range backRepoOther_appearance.Map_Other_appearanceDBID_Other_appearanceDB {

		// next line of code is to avert unused variable compilation error
		_ = other_appearanceDB

		// insertion point for reindexing pointers encoding
		// update databse with new index encoding
		query := backRepoOther_appearance.db.Model(other_appearanceDB).Updates(*other_appearanceDB)
		if query.Error != nil {
			log.Fatal(query.Error)
		}
	}

}

// BackRepoOther_appearance.ResetReversePointers commits all staged instances of Other_appearance to the BackRepo
// Phase Two is the update of instance with the field in the database
func (backRepoOther_appearance *BackRepoOther_appearanceStruct) ResetReversePointers(backRepo *BackRepoStruct) (Error error) {

	for idx, other_appearance := range backRepoOther_appearance.Map_Other_appearanceDBID_Other_appearancePtr {
		backRepoOther_appearance.ResetReversePointersInstance(backRepo, idx, other_appearance)
	}

	return
}

func (backRepoOther_appearance *BackRepoOther_appearanceStruct) ResetReversePointersInstance(backRepo *BackRepoStruct, idx uint, other_appearance *models.Other_appearance) (Error error) {

	// fetch matching other_appearanceDB
	if other_appearanceDB, ok := backRepoOther_appearance.Map_Other_appearanceDBID_Other_appearanceDB[idx]; ok {
		_ = other_appearanceDB // to avoid unused variable error if there are no reverse to reset

		// insertion point for reverse pointers reset
		// end of insertion point for reverse pointers reset
	}

	return
}

// this field is used during the restauration process.
// it stores the ID at the backup time and is used for renumbering
var BackRepoOther_appearanceid_atBckpTime_newID map[uint]uint
